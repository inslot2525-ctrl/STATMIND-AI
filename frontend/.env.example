# Set to your Railway backend URL after deploy
# This is read at build time on Vercel — also set it in Vercel dashboard > Settings > Environment Variables
VITE_API_URL=https://statmind-api.up.railway.app
